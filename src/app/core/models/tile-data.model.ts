export interface ITileData {
    id: number;
    title: string;
    priceInfo: string;
    description?: string;
    extra?: string;
    icon?: string;
    iconBackground?: string;
    checked?: boolean;
}